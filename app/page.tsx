// Placeholder landing page. Phase 1 replaces this with a redirect
// to /book/[DEFAULT_SLUG] once the reader exists.
export default function Home() {
  return (
    <main className="flex flex-1 items-center justify-center bg-[#efe6d3] p-8">
      <div className="text-center text-stone-700">
        <h1 className="text-3xl font-semibold">Adventure Book</h1>
        <p className="mt-2">Buku kenangan kita — segera hadir. 💌</p>
      </div>
    </main>
  );
}
